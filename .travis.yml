language: node_js
node_js:
  - 12
services:
  - mysql
before_install:
  - mysql -e 'CREATE DATABASE ecorewards;'
install: npm install
before_script:
  - npm run bdd-setup
  - npm start &
scripts:
  - npm run report-coverage
  - npm run bdd
